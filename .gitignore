# Superpowers brainstorm/visual-companion artifacts (local only)
.superpowers/

# OS
.DS_Store

# macOS signing/notarization credentials (local only; see .env.notarize.example)
.env.notarize

# Node / SvelteKit
node_modules/
build/
.svelte-kit/
dist/

# Tauri / Rust
src-tauri/target/
