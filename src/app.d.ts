// Ambient type declarations for the app.

// Injected at build time by Vite (`define` in vite.config.ts) from package.json's
// `version`, e.g. "0.1.10". Replaced with a string literal at build time; see
// $lib/settings/version.ts for how it becomes the Settings footer label.
declare const __APP_VERSION__: string;
