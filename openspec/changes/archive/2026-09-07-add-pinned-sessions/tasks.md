# Tasks

- [x] 1.1 Add `pinned: string[]` to the durable `ui` prefs with parsing,
      `isPinned` / `togglePinned` / `forgetPinned`, and unit tests.
- [x] 1.2 Add the pure `pinRowsToTop` roster helper with unit tests.
- [x] 1.3 Render a Pinned group above the lanes in `Inbox.svelte`, mark pinned
      rows with a pin glyph, add Pin to top / Unpin to the row context menu, and
      unpin on every permanent-delete path.
- [x] 1.4 Add `pin` / `pin-off` glyphs to the icon set.
- [x] 1.5 `yarn check` and `yarn test` green.
