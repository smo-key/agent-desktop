# Tasks

- [x] 1.1 Extend `CompactModeStore` to a `density` enum (`default | compact |
      minimal`) with `DENSITIES`, `enabled`/`minimal` getters, `setDensity`, and
      legacy `{ enabled }` migration in `parseCompactModePrefs`; unit tests.
- [x] 1.2 Add the "Minimal" option to the Settings Density dropdown.
- [x] 1.3 Render minimal rows in `Inbox.svelte`: title only, icon 20px, no
      sub-line or meta line, tighter padding.
- [x] 1.4 `yarn check` and `yarn test` green.
